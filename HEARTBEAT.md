# HEARTBEAT.md

- Review latest `memory/YYYY-MM-DD.md` entries since last heartbeat.
- If durable facts/preferences/decisions appear, append atomic facts into `life/**/items.json`.
- Update corresponding `life/**/summary.md` (Hot/Warm/Cold recency emphasis; no deletions).
- Ensure outdated facts are marked `superseded` with `supersededBy` links instead of removing.
- Keep `TACIT.md` updated only when clear behavioral patterns change.
- If nothing important changed, reply HEARTBEAT_OK.
